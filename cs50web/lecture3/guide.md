# Lecture 3 Django
Django like, Flask is used to create web apps.
---
The syntax for starting a new web app project is 
```
django-admin startproject [project name]
```
***A django web app is made of apps***<br>
The syntax for creating a new app is:<br>
```
django manage.py [app-name]
```

**There is a bug. the line 31 of tasks.views.
- *fixed*.
